ARCHPR 壓縮包暴力破解

010中灰色的區域為數據區，粉色的區域是目錄區

黃色為原文件目錄區標頭





遇到加密zip檔，首先判斷是否為偽加密

**group-1（客戶端 IP）**：`120.196.145.58` — 發起請求的使用者 IP 位址。

**group-2 & group-3（身份識別與使用者名稱）**：`-` — 未提供 RFC 1413 身份識別與 HTTP 認證使用者名稱（顯示為空白）。

**group-4（請求時間）**：`11/Dec/2013:10:00:32 +0800` — 伺服器收到請求的時間點及時區（UTC+8）。

**group-5（請求行）**：`GET /__utm.gif HTTP/1.1` — 使用 GET 方法請求 Google Analytics 追蹤像素檔案 `/__utm.gif`，通訊協定為 HTTP 1.1。

**group-6（HTTP 狀態碼）**：`200` — 請求成功（OK）。

**group-7（傳送資料大小）**：`35` — 伺服器回應給客戶端的資料主體（Body）大小，單位為位元組（Bytes）。

**group-8（來源頁面 Referer）**：`[http://easternmiles.ceair.com/flight/index.html](http://easternmiles.ceair.com/flight/index.html)` — 使用者發起請求前所在的頁面（東方航空東方萬里行機票頁面）。

**group-9（使用者代理 User-Agent）**：`Mozilla/5.0 ... Chrome/31.0...` — 使用者的瀏覽器與作業系統資訊（Windows 7，Chrome 31）。

**group-10（Cookie / 自訂標頭資訊）**：`BIGipServermu...; uuid=...; userId=099; st=1` — 包含 F5 負載平衡器 Cookie、Webtrends 追蹤碼、使用者 UUID 及 UserId。

**group-11 & group-12（回應時間或伺服器效能數據）**：`1482` 與 `352` — 通常代表伺服器處理請求的時間（毫秒/微秒）或傳輸資料量細節。